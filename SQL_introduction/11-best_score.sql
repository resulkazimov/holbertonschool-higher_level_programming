-- Lists all records with a score >= 10 in the table second_table
-- SQL query to list score and name, filtered and ordered
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
